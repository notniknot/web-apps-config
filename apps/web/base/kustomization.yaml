apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - external-secret.yaml
  - httproute.yaml
  - image-updater.yaml
  # The preview template is applied alongside the app so the preview UI can read
  # (from the live cluster object) which values are overridable.
  - preview.yaml
  - redis.yaml
  - s3-persistent-volume.yaml
  - s3-persistent-volume-claim.yaml
  - service.yaml

images:
  - name: ghcr.io/notniknot/web-apps
    newTag: main
