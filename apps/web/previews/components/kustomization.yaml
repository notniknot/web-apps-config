apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - preview-demo-configmap.yaml
  # Generated per-preview secret (applied by the preview app into its namespace).
  - generated-preview-secret.yaml
