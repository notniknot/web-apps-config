apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Previews of the staging env: same shared preview deltas layered on the
# rendered staging overlay. Referenced by the web-staging PreviewTemplate.

resources:
  - ../../staging

components:
  - ../component
