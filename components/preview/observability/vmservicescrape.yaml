apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: web
  labels:
    app.kubernetes.io/name: web
    preview.sonia.so/plumbing: metrics
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: web
  endpoints:
    - port: http
      path: /metrics

